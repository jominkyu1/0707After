/*  1. 열거타입도 레퍼런스(참조)형에 해당 (창조타입 종류: 클래스, 배열, 인터페이스, 열거타입)
    2. 한정된 값만을 갖는 데이터 타입이 열거타입이다.
        ex) 요일, 계절 등
    3. 열거타입의 명명방식은 클래스처럼
* */
public enum Week {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY;
        //상수(대문자)
}